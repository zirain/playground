apiVersion: telemetry.istio.io/v1alpha1
kind: Telemetry
metadata:
  name: mesh-default
spec:
  # accessLogging:
  #   - providers:
  #       - name: envoy
  metrics:
  - providers:
    - name: prometheus