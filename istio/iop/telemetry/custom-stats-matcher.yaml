apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  values:
    global:
      meshID: mesh1
  meshConfig:
    accessLogFile: /dev/stdout
    defaultConfig:
      proxyStatsMatcher:
        inclusionRegexps:
          # - ".*circuit_breakers.*"
          # - ".*upstream_rq_xx.*"
          - ".*ssl.*expiration_unix_time_seconds"
        # inclusionPrefixes:
        #   - "upstream_rq_retry"
        #   - "upstream_cx"
