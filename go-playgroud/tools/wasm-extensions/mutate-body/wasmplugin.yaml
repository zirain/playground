apiVersion: extensions.istio.io/v1alpha1
kind: WasmPlugin
metadata:
  name: mutate-body
  namespace: default
spec:
  selector:
    matchLabels:
      app: httpbin
  url: oci://ghcr.io/zirain/mutate-body:latest